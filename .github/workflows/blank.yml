# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "master" branch
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'
        
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      
      - name: Run test
        run: ./gradlew test
        
      - name: Run build
        run: ./gradlew clean build -x test

      - name: Make zip file
        run: zip -r ./$GITHUB_SHA.zip .
        shell: bash

      - name: Upload build file to NCP
        env:
          AWS_ACCESS_KEY_ID: WqCxdTAHmUzbh2FAV9Wz
          AWS_SECRET_ACCESS_KEY: AQJVNnk74avEr2xSTv3R3mFwmbNNu2qegWRzEgpl
        run: aws --endpoint-url=https://kr.object.ncloudstorage.com s3 cp ./$GITHUB_SHA.zip s3://hakjun-bucket
#          aws --endpoint-url=https://kr.object.ncloudstorage.com
#          s3 cp --recursive ./$GITHUB_SHA.zip
#          s3://hakjun-bucket

